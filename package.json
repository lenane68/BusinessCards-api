{
  "name": "json-server-api",
  "version": "1.0.0",
  "packageManager": "npm@9.6.7",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
